<%= form_for @user do |form| %>
  
  <div class="#">
    <%= form.label :photo %><br>
    <%= form.file_field :photo, multiple: false %>
  </div>

  <%= render partial: '/users/form', object: form %>

  <div class="submit-field">
    <%= form.submit %>
  </div>
<% end %>